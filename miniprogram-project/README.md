# 小毛毛微信小程序

原生微信小程序客户端，复用仓库中的 Express API，与现有 Web 端共享内容、分类和媒体数据。

## 本地打开

1. 用微信开发者工具导入 `miniprogram-project/`。
2. 将 `project.config.json` 中的 `appid` 改成实际小程序 AppID。
3. 首次使用时复制本地配置：

```bash
cp config.sample.js config.js
```

4. 将本地 `config.js` 的 `apiBaseUrl` 改成实际 API 地址，例如 `https://example.com/api`。`config.js` 已加入 `.gitignore`，后续 `git pull` 不会覆盖你的本地配置，也不会误提交真实地址。
5. 在微信公众平台按实际使用能力配置对应的 HTTPS 合法域名。
6. 开发阶段如需临时跳过域名校验，可在开发者工具本地设置中关闭合法域名检查；正式上传前必须恢复真实域名配置。

## 审核模式

后台“系统设置 → 小程序审核模式”开启后，小程序会按审核配置进入对应的展示状态。首页和笔记详情继续展示可用内容，其他页面根据当前审核配置决定是否展示；配置读取异常时默认保持审核模式。服务端会同步校验小程序请求，确保前后端状态一致。

## 当前范围

- 首页分类 + 两列瀑布流
- 笔记图文 / 视频详情
- 家庭账号体系
- 个人内容页
- 小程序审核模式
