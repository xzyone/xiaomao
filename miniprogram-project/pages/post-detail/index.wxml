<view class="detail-page" wx:if="{{post}}">
  <view wx:if="{{post.type != 2 && displayImages && displayImages.length}}" class="media-stage" style="height: {{currentMediaHeight}}rpx;">
    <swiper class="media-swiper" style="height: {{currentMediaHeight}}rpx;" indicator-dots="{{displayImages.length > 1}}" circular="{{displayImages.length > 1}}" bindchange="onMediaChange">
      <swiper-item wx:for="{{displayImages}}" wx:key="*this">
        <image class="detail-image" src="{{item}}" mode="aspectFit" lazy-load data-index="{{index}}" data-url="{{item}}" bindload="onDetailImageLoad" bindtap="previewImage" />
      </swiper-item>
    </swiper>
    <button wx:if="{{currentImageCanShowOriginal}}" class="original-image-button" catchtap="showOriginalImage">{{ui.labels.detailOriginal}}</button>
  </view>
  <video wx:if="{{post.type == 2 && post.video_url && post.poster_url}}" class="detail-video" src="{{post.video_url}}" poster="{{post.poster_url}}" controls object-fit="contain" show-mute-btn="{{true}}" show-fullscreen-btn="{{true}}" show-play-btn="{{true}}" show-center-play-btn="{{true}}" enable-progress-gesture="{{true}}" vslide-gesture="{{true}}" vslide-gesture-in-fullscreen="{{true}}" />
  <video wx:elif="{{post.type == 2 && post.video_url}}" class="detail-video" src="{{post.video_url}}" controls object-fit="contain" show-mute-btn="{{true}}" show-fullscreen-btn="{{true}}" show-play-btn="{{true}}" show-center-play-btn="{{true}}" enable-progress-gesture="{{true}}" vslide-gesture="{{true}}" vslide-gesture-in-fullscreen="{{true}}" />

  <view class="post-author-header">
    <image class="avatar" src="{{post.user_avatar || '/assets/avatar.png'}}" mode="aspectFill" lazy-load binderror="onPostAvatarError" />
    <view class="author-copy">
      <text class="nickname">{{post.nickname || ui.labels.anonymousUser}}</text>
    </view>
  </view>

  <view class="detail-content">
    <text class="title">{{post.title}}</text>
    <text class="content">{{contentText}}</text>
    <view class="tag-row" wx:if="{{post.tags && post.tags.length}}"><text wx:for="{{post.tags}}" wx:key="id" class="tag">#{{item.name}}</text></view>
    <text class="date">{{displayDate}} · {{ui.labels.detailViews}} {{post.view_count || 0}}{{post.location ? ' · ' + post.location : ''}}</text>
  </view>

  <view wx:if="{{!auditModeEnabled}}" class="comments-section">
    <text class="section-title">{{ui.labels.detailComments}} {{post.comment_count || 0}}</text>
    <view wx:for="{{comments}}" wx:key="id" class="comment-item">
      <image class="comment-avatar" src="{{item.user_avatar || '/assets/avatar.png'}}" mode="aspectFill" lazy-load data-index="{{index}}" binderror="onCommentAvatarError" />
      <view class="comment-body">
        <text class="comment-user">{{item.nickname || ui.labels.anonymousUser}}</text>
        <text class="comment-content">{{item.content}}</text>
        <text class="comment-meta">{{item.user_location ? ui.labels.detailIpPrefix + item.user_location : ''}}</text>
      </view>
    </view>
    <view wx:if="{{!comments.length}}" class="no-comments">{{ui.labels.detailEmptyComments}}</view>
  </view>
  <view wx:if="{{!auditModeEnabled}}" class="comment-bar safe-bottom">
    <input class="comment-input" value="{{commentText}}" placeholder="{{loggedIn ? ui.placeholders.detailComment : ui.placeholders.detailCommentLogin}}" bindinput="onCommentInput" bindtap="handleCommentInputTap" />
    <button class="send-button" bindtap="submitComment">{{ui.labels.detailSend}}</button>
  </view>
</view>
<view wx:elif="{{loading}}" class="loading-page">{{ui.labels.loading}}</view>
