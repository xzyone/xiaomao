<view class="bottom-nav safe-bottom">
  <view class="nav-item {{current === 'home' ? 'active' : ''}}" bindtap="goHome">
    <text class="nav-icon">⌂</text>
    <text>{{labels.navHome}}</text>
  </view>
  <view wx:if="{{!readonlyModeEnabled && loggedIn}}" class="publish-button" bindtap="goPublish">＋</view>
  <view class="nav-item {{current === 'profile' ? 'active' : ''}}" bindtap="goProfile">
    <text class="nav-icon">○</text>
    <text>{{labels.navProfile}}</text>
  </view>
</view>
